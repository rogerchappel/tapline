# Sample report

Regenerate this example with:

```sh
npm run build
node dist/cli.js inspect examples/fixtures/sample-tap --output examples/output/sample-report.md
```

The checked-in placeholder keeps the directory visible; generated reports are deterministic in shape but include the current timestamp.
