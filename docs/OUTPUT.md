# Output Formats

Tapline supports two MVP output formats.

## Markdown

Markdown is the default. It is intended for PR comments, release review packs, local notes, and maintainer handoff.

## JSON

JSON is intended for tests, scripts, agents, and future integrations. It includes tap metadata, checklist items, validation command plans, optional command results, release notes, and generation time.
